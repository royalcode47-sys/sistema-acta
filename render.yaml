services:
  - type: web
    name: sistema-acta-aire
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --chdir servidor server:app --bind 0.0.0.0:$PORT
    envVars:
      - key: FLASK_ENV
        value: production
      - key: PYTHON_VERSION
        value: 3.11.0
